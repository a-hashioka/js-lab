export const ROTATION_SPEED = 0.005;

export const AUTO_ROTATION_SPEEDS = {
  x: 0.005,
  y: 0.008,
  z: 0.003,
};

export const PERSPECTIVE = {
  fov: 500,
  viewDist: 1000,
};

export const PHI = (1 + Math.sqrt(5)) / 2;
